# M6f — individual native summaries and audit presentation

Actual noisy executor probe `noise-load-probe-7120c99405` passed: 72,814 tool data
tokens, two native replacements, two separate Mongo audit records, and two exact
provider-summary joins. A summary contained the real noise before its removal;
the task completed. This is a narrow production-route probe, not full A02.

Previously, the raw native receipt preserved every replacement, but the Python
Mongo projection selected only the last one. The new adapter records each result,
its own native bracket events, and exact actual request/response references. Old
evidence is unchanged; the correction identifies its scope. Three preserved real
Gemma summaries also matched the new join logic in a separate replay check.

The static audit now shows actual summary input beside its original range and
summary, source references, measured provider timing/usage, and real parent-version
diffs. Unknown fields stay null. State proposals preserve the model's reason and
change class; failed CAS writes have explicit CONFLICT events. A saved revision
is distinguished from a committed state-head pointer. JSON is a plain companion
file; model/user text remains escaped and no scripts/CDNs are loaded.

In-app Browser verified the new revision and compaction displays, including an
opened actual summary request containing the noise marker. Machine JSON navigation
returned `ERR_BLOCKED_BY_CLIENT`; an earlier recording script incorrectly wrote
PASS before checking that tool error. Both that record and its explicit correction
are retained; the replacement UI result is INCONCLUSIVE for export. No browser
download completion is claimed. Synthetic UI actions supply no human cognition
votes. Screenshots were individually visually reviewed before export allowlisting.

The exporter now also sanitizes embedded JSON data URLs in retained early UI
attempts. Real archive I/O verifies ordinary text, nested frozen archives and
embedded JSON redaction while preserving reviewed image bytes. Unreviewed or
changed images are rejected. A probe script syntax failure is retained separately.

Validation: `check-20260920T001024Z-4d56a6`, 58 passed, exit 0, including the exact
19-conflict assertion in the 20-way CAS test and persisted proposal rationale.
The previous targeted checks and all failed attempts remain. Detailed SHA256,
commands and exit codes are in M6f.json and the referenced result artifacts.

Full A01 and A02 are running with immutable experiment manifests. Full L02/L03/L12
are queued in sequence with matched Qwen-only controls. F01 repeat, F02 under quiet
load, L09/L11 and human quality review remain pending. No model services, old
database data, or default DSH profile were changed by this milestone.
