# V1 最终交付：本机核心主线

日期：2026-09-20。按 ADR-002 的 P1 → P2 → P3 主线完成有限实跑验证；本机入口已实跑。按用户最后要求，运行配置、依赖、运行数据与记录原样归档到工作区外，工作区只保留可提交内容；再次启动前需从 ZIP 恢复本机运行文件。此结论不替代旧版全面验收矩阵，也不宣称所有语义判断正确。

## 打开与停止

```powershell
C:\workspace\asuna_cognition_core_v2\start-asuna.cmd
```

恢复步骤见 [RUN_ASUNA.md](../RUN_ASUNA.md)。恢复后输入中文即可，/trace 查看真实过程，/new 更换角色上下文但保留数据库记忆，/compact 请求下一完整轮次边界的原生压缩，/quit 退出。当前验证进程已正常结束，不留后台任务。

Gemma 31B 使用本机 8083，部署容量 68,608；Qwen 行动脑与固定 DSH 0.1.5-rc.2 沿用现有路由和版本。

## 有哪些实际证据

| 阶段 | 实际观察 | 原文与限制 |
| --- | --- | --- |
| P1-A | 持续本机角色聊天 | [P1-A 报告](P1-A-REPORT.md) |
| P1-B | 聊天→自主行动→结果→继续聊天 | [P1-B 报告](P1-B-REPORT.md) |
| P1-C | 新聊天入库、索引、更正与召回 | [P1-C 报告](P1-C-REPORT.md) |
| P1-D | 单次原生压缩／恢复与场景隔离 | [P1-D 报告](P1-D-REPORT.md) |
| P2 | 真实工具错误在原行动回路中恢复 | [P2 报告](P2-REPORT.md) |
| P3-A/B | 角色选择、Qwen 实现技能，跨恢复发现与复用 | [P3-A/B 报告](P3-AB-REPORT.md) |
| P3-C | 角色形成理解修订、恢复使用与另一人物隔离 | [P3-C 报告](P3-C-REPORT.md) |

P3-C 中，Gemma 将“用完备证据建立安全感”的旧做法修正为对当前人物“结论前置，细节后置”，程序提交了有来源的关系版本。重启后的回复实际变得直接；另一人物要求步骤解释时得到详细回答，其上下文没有 A 的私聊理解。实际前后正文见 P3-C 报告。

## 一页真实缺口

- P1-A/C/D 的早期实跑使用旧 Gemma 26B；切换 31B 后复查了持续聊天、行动期间聊天、技能往返和理解恢复，没有重跑完整 C1–C4。
- 已观察到原生压缩摘要的偏好归属和引用误差；没有验证自动阈值压缩或任意长时间连续运行。
- Gemma 31B 当前保留 native_thinking=false；独立有界 MONOLOGUE/DECIDE/可选 REFLECT/SPEAK 仍由程序推进。
- 技能是规则驱动的同类笔记核对。首次生成约1196秒、复用约177秒，行动延迟明显。
- B 的假设题回答包含未完全确定的时间顺序推断；一次状态更新不证明所有认知判断正确。
- 媒体表达与 QQ/V2 未验证，独立人物场景使用正式路由的本机探针；未做 QQ 实际收发。
- 包内数据库快照用于审阅与追溯，不是完整 Mongo 备份。外部 Mongo、模型服务与 WSL 环境未打包或清理；运行仍依赖这些现有服务。

## 最终提交数据

- 最终原样 ZIP：`C:\workspace\asuna-v1-core-raw-20260920.zip`。内部顶层为 `asuna_cognition_core_v2/`，保留工作区原相对路径；仅排除 `.git`，文件内容不做脱敏或替换。
- 包含源码、配置示例、实际提示词、启动脚本、原始 `config/local.json`、完整本地 `.runtime`（含 DSH homes、技能和任务文件）、依赖、缓存及全部 `reports`。内部依赖目录链接展开为普通目录，文件内容保持原样。包内旧的脱敏导出只是此前记录，以本次外部原样 ZIP 为最终交付。
- 机器结论在包内 `reports/final-v1-20260920/result.json`；同目录 `database_snapshot.json` 包含人物／场景、版本、对话、记忆、行动、工具和审计记录。
- 包内 `workspace-status.txt`、`changes.patch` 记录归档前的工作树；快照包含此前阶段与用户原有文档迁移，不将它们全算作本轮新改动。
- 归档时逐文件记录并验证内容，校验结果在 ZIP 旁的 `asuna-v1-core-raw-20260920.validation.json`。验证成功后清理 `.runtime`、`.venv`、`node_modules`、`reports`、缓存、本机配置和生成清单，保留 `.git`、源码、测试、文档、配置示例及锁文件。

没有执行 Git 提交或推送，没有生成 QQ 适配器。V2 由用户下一步主动启动。
