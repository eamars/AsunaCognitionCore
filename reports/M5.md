# M5 native compaction and request budgeting

`M5-20260918T143228Z-7e001a` passed an actual Gemma summary call through native
DSH BasicCompactionEngine. The bridge selects all closed episode surface nodes
at the next real phase's pre-step, before the new user message enters history;
native DSH validates pairing, records brackets, validates shrink, replaces the
range and flushes it. Only the documented summarizer hook is overridden. It
preserves source/epistemic/scope/task distinctions instead of the default coding
summary instructions. No fabricated SDK compact API or injected mock summary.

`compact` queues work at a complete boundary; it does not claim an immediate
summary. It rejects an incomplete character episode. Compaction generations are
recorded independently for each lane. Startup attempts `105e1c` and `418c75`
failed because sdk-minimal omits tokenMeter; the explicit native dependency was
then mounted. These failed attempts remain intact.

After these changes `M2-20260918T143835Z-31e598` passed native restart again, and
`M3-20260918T143427Z-6230d1` passed the two-model file route. The later
`M3-20260918T144114Z-b9152a` additionally measured every input through actual
server tokenization: all 11 measured counts exactly matched returned
prompt_tokens, covering Gemma and Qwen tool continuation. Gemma exposes rendered
template and token IDs; Qwen's count endpoint requires equivalent Anthropic
conversion, and final OpenAI rendered token IDs are not exposed. This limitation
must remain in deployment conclusions.

Every generation now checks measured input + full output budget + 4096 margin
against the observed 262144 server metadata limit, and applies the 65536/196608
normal working budgets. Explicit capacity probes may bypass only the working
budget. Matching metadata and small requests are not proof of 196k/234k capacity.
An OS file lock serializes each local inference endpoint across Asuna processes.

Not yet established by this probe: repeated compaction matrix, failure/restart
at every native bracket, large-context capacity, semantic retention threshold,
or human-rated character quality. Soft auto-compaction is not yet enabled;
over-budget requests fail closed until an explicit safe compaction is requested.
