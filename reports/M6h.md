# M6h — independent review transport, report integrity and complete export probe

The latest frozen integration check `check-20260920T005215Z-7e0bed` passed all
58 tests (exit 0). The matching engineering assessment again records 23 PASS
and E01 INCONCLUSIVE; the missing historical isolation snapshot is not repaired
by rerunning later probes. The preceding targeted check passed eight tests.

Human-review packs now reconstruct L06 reflection sources from the actual hashed
provider request, display the untouched reflection and subsequent public speech,
and map L08 answers to scene/repetition/case. A02's nested scenario is used for
its actual context. The operator attribution map is hash-bound into new packs.
Human import remains fail-closed on tampering or missing attestation. New scoring
arithmetic reports each model separately, retains missing votes and disagreements,
and cannot average away a critical error. Nine explicit synthetic arithmetic and
real blank-pack checks passed; these contribute zero human cognition votes.

In-app Browser verified the reflection page with actual preserved material,
original no_change rationale and later speech. No rating was assigned and the
human attestation remained false. Screenshots were visually reviewed. The earlier
Browser download limitations and corrections remain; no new download claim is made.

The real endpoint-timeout probe `fault-capture-probe-c36ad8571d21` passed the
mechanical recovery/evidence checks. L11 now preserves terminal task state even
on failure and generates review material alongside original responses. Its full
12-case matrix has started. The L09 hardest-condition reprobe is still running.

Report previews preserve all 41 contract IDs, past failures and paused/incomplete
matrices. A started matrix is INCONCLUSIVE, not an unattempted or passing test.
The original package report validator passed with `--allow-incomplete`; the V1
report itself correctly remains FAIL/INCONCLUSIVE. Explicit illustrative traces
include a successful real reconciliation task and a valid-protocol P06 failure
to choose the required delegate route, plus the deliberate E22 missing-audit case.
The normal trace's currency wording still requires human review.

The complete export probe successfully wrote and verified 24,878 files in
`evidence-m6h-checkpoint.zip`, including reviewed Browser images and nested frozen
inputs. Every archived file was reread and hash checked. It is a point-in-time
checkpoint during ongoing matrices, not the final `evidence.zip`; its manifest
defines the exact included bytes. The large archive stays outside Git, with its
digest committed. No live experiment result was replaced by this snapshot.

An actual Git-index comparison detected that inherited `core.autocrlf=true`
normalized the tracked environment fingerprint bytes. The failed comparison is
retained. Repository-local attributes now preserve exact bytes across platforms;
tracked evidence is restaged from the original workspace bytes. This changes Git
storage, not the original frozen evidence or any model/server configuration.

Estimated implementation/acceptance-delivery work completion is about 80%, not
an acceptance pass rate. Full long matrices, quiet-load F01/F02, independent human
review and final report/export remain. No actor/executor grades its own output.
