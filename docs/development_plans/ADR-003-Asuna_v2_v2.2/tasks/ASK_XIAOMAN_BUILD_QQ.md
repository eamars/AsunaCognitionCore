# 通过已可用的本机入口交给小满

下面是一段自然语言目标，**由运行中的Asuna处理**，不是绕过角色直接给Qwen的编程题。Codex先把真实宿主说明与配置路径填写在正常可读位置；不把token正文复制进聊天。

---

小满，我希望以后能从QQ来和你聊天。现在请用已经配置好的本地NapCat，给自己开发一个可持续使用的接口，并实际接通。

宿主已经给你可调用的入站、公开出站、发送回执和受管理试运行接口，实际说明在这次任务的RUNTIME_API.md。连接配置由程序管理；需要的文档与权限已经按这个任务开放。你可以自己选择实现、写代码、试运行和修错，不用每一步问我。

先只接配置里允许的私聊，不改旧小满实例，也不要自动接入全部群。QQ来消息要进入你现在的角色、记忆和行动流程，不另写一个回显机器人。你说出的内容由宿主公开出口发回原对话，别让行动脑直接替你写台词。

程序要能持续接收，重启后还能用；不用你每次手挂一个等待脚本。实现和用法放进自己的持久技能目录。普通报错继续自己修，缺配置或宿主真的缺接口时再说明具体问题。

请现在开始实现和试用。完成后告诉我从哪里发第一句话，哪里能看实际错误，以及还不能做什么。开发期间，我们仍然可以在这里聊天。

---

代码/资料在真实任务授权目录中准备，不给预写NapCat成品。需要登录或选择真实测试账号时再由用户一次确认。小满能得到实际使用说明和错误，不需要哈希证书或所有历史测试文件。
