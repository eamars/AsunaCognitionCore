# M6i — durable transport keepalive and native failure provenance

The actual installed DSH client survived 428.75 seconds of queued upstream wait
with 28 audited SSE comment heartbeats, exactly one generation and unchanged
output (`queue-header-probe-60b30bcb90`, exit 0). These comments contain no model
content. The earlier 303-second probe was insufficient: L11 subsequently exposed
a native transport failure after about 305 seconds, while its real upstream call
was still queued. That failure, the narrower old claim and the correction remain.

Five own experiment trees were stopped, with 16 owned processes stopped and no
remaining owned process. All five in-flight databases were recovered through
read-only observers after exact persisted evidence-root ownership checks. The
initial process guard refusal and every completed/failed/paused sample are kept.
Shared model servers and deployment launch settings were not modified. Paused
matrices will not be resumed under changed implementation hashes.

Native pre-step compaction may reject a summary before the pending user message
enters the conversation surface. The bridge now returns the exact native error
correlated through accepted inbox message, turn start and turn end; it never
manufactures normal output. The installed-engine rejection probe passed with
empty content, no applied compaction, and idempotent replay without another
provider call (`compaction-error-probe-f962c24e13dc`, exit 0).

Three successive F02 plumbing attempts are retained. The first revealed a
summary larger than its tiny history, correctly rejected by DSH but previously
hidden by the adapter. The second exposed an ambiguous synthetic measurement
instruction and Gemma exhausting 4096 tokens without content. The revised probe
uses the immutable actual persona prefix and one unambiguous batch-label fact.
`performance-probe-7640d0079a34` passed all 26 real-model calls and two actual
native summaries (exit 0). Fixed-prefix pilot reuse was 99.34% Gemma and 98.32%
Qwen; one hot repetition per model is not the formal ten-replay cache result.
No acceptance threshold, persona fixture, model sampling or server parameter
was changed. The formal F02 plan measures 166 private turns, cold and hot A-B-A
cycles separately, plus two summaries; missing provider counters remain null.

L09 now emits review material for actual task evidence and key memories/public
promises. Condition identifiers remain in the operator manifest. Eleven review
arithmetic checks passed, including the missing-condition counterexample; every
test vote is explicitly synthetic and contributes zero independent human votes.

Latest frozen regression: `check-20260920T013217Z-56820f`, 58 passed, exit 0.
Matching engineering assessment: `engineering-assessment-fba1ab29e45e`, 23 PASS,
E01 INCONCLUSIVE for the absent pre-implementation historical snapshot. The
earlier 58-pass regression in this stage is also retained. Package integrity and
the immutable 41-case fixture remain unchanged. Full F01/F02 and the restarted
long matrices are next; no full V1 or cognition acceptance is claimed here.
