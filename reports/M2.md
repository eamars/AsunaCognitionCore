# M2 native DSH lifecycle probe (superseded deployment)

`M2-20260918T141400Z-e11988/result.json`: PASS, real Mongo and Gemma through
DSH 0.1.5-rc.2. Two episodes completed MONOLOGUE → DECIDE → SPEAK, with a fresh
DSH process resuming the same durable session between episodes. Six captured
provider requests contained the persona and scoped relationship; no tools or
Qwen calls. Both responses went through PublishService and the local receipt sink.

Earlier attempts are preserved: `ff3d3a` failed because installed runtime and
lock were 0.1.1-rc.2 despite package.json declaring 0.1.5-rc.2; `a6f5f9` failed
on Windows fsync of a read-only descriptor. The former is consistent with an
earlier npm process finishing after cancellation, but this cause was not proven.
Boot now requires matching declared, locked and installed versions and records
executable, lock and bridge hashes. The descriptor now opens with write access.

The official Python SDK initializes and shuts down the pinned DSH runtime. Its
public server protocol lacks durable resume and compaction; `runtime.ts` uses
the real native AgentRegistry create/resume, Session.snapshotEvents and
SessionRegistry.flush APIs. Stable operation records reconcile a delivered
message against the durable turn, and unknown delivery stops instead of retrying.
This probe covers a clean process restart, not every forced crash boundary.

The user subsequently requested latest DSH. This result is retained as historical
evidence; new deployment and experiment IDs must be used for the upgrade.
