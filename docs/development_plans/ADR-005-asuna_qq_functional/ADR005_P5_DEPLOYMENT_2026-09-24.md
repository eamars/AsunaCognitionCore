# ADR-005 P5 部署记录

2026-09-24 23:20（Pacific/Auckland），小满交付的 `P5_TOPIC_PROACTIVE.patch` 已干净应用到现有宿主，并通过 `start-asuna.cmd --port 8767` 重载。正式 Web 工作台恢复可打开。产品代码、测试和使用说明由小满编写；Codex 仅机械应用与维护重载，没有另做产品验收。

小满报告其离线自测 19/19；在打补丁后的开发树上 P5 19/19、P2 24/24，P2 原边界 42/42 与闭环 21/21 也保持通过。她在自测中修复了主动再核误拦普通群 @ 回合的问题，并使可选主动逻辑出错不拖垮原聊天回合。

主动模式需要在某个**明确选择的已授权群路由**设置 `proactive.enabled`。2026-09-25，用户指定群 `1002866238`，Codex 仅在该群路由打开此开关并维护重载；其他群仍关闭。没有另写触发词或节奏参数，使用小满实现的默认配置。正式 Web 恢复，且已通过 Web 告知小满：按群内实际氛围自然参与，避免测试口吻；闲聊、调情和成人向话题由她自行判断。真实 Mongo 测试、真模型通道和真人群舒适度不在小满这轮自测证据内；Web 恢复不代表这些已验证。

ADR-005 的 P3（日常自由定时）随后也已部署，见 [P3 部署记录](ADR005_P3_DEPLOYMENT_2026-09-25.md)。
