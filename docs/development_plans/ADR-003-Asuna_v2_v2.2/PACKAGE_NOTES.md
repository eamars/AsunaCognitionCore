# 本包检查与使用边界

这是开发任务和验收对话，不含已完成的NapCat适配器，也不包含虚构执行结果。

本次已检查：Markdown内部文件链接存在；JSON可解析；私有连接候选默认关闭、用户和群授权为空；固定token仅出现在私有候选配置中；归档能完整读取。没有建立哈希验收流水线。

尚未执行：V2.2宿主API、QQ连接、任何模型调用、数据库写入、DSH timer桥、群场景验证或Bonus总结提取。接口名和新增启动命令均为任务要求，由Codex在本机实现并交付实际说明。

源码发现来自用户最新V1归档；实际运行结论来自该归档的已有报告和代表记录，而非本次重跑。独立裸DSH原型仅作为风险参考。

本包包含用户授权保存的局域网token候选，整个ZIP应按私有配置资料保管，不要公开发布。候选不证明端点仍有效，首次配置仍需本地核实登录账号与测试对象。
