# 我的 Asuna：本机启动与使用

> 给 Codex：这是待填写模板，不是用户当前可执行说明。接通后另存为仓库的 `RUN_ASUNA.md`。完成项填真实值，未完成项明确写未完成；不得编造已验证命令。交用户的完成入口不得留路径占位符。

## 当前可用状态

本机聊天：尚未由本包实机验证。
工具往返：尚未由本包实机验证。
新聊天记忆：尚未由本包实机验证。
基础成长：尚未由本包实机验证。
QQ 接入：V2任务，尚未开始／尚未由本包实机验证。

## 我在哪里启动

用户实际使用的操作系统／终端：Codex根据本机填写。
Asuna 实际运行位置（本机／WSL／容器／已有主机）：填写。
唯一常用启动脚本的实际路径：填写。
一条已实跑的启动命令：填写，不能只有 `asuna chat` 这个设计名称。
一次性准备（已做了什么；以后还需什么）：填写。正常启动不重复安装或探测。

## 正常怎样用

启动后直接自然语言输入，不需要 JSON，不分别打开两个模型，不手动启动 executor 或 index。
当前角色显示名：填写；应用名 Asuna 不等于必须用这个名字扮演角色。
查看这次过程与错误的真实命令：填写。
退出当前输入窗口的方法及实际效果：填写。
停止整个应用/服务的方法：填写，不误杀旧实例。
重开后恢复哪些内容：填写；未完成任务如何处理也要如实说明。

## 配置在哪里

有效配置路径、Asuna数据库名、模型路由名称：填写；不含密码/token正文。
允许任务工作的目录：填写。
自己的持久技能目录：填写。
原始日志／traceback如何打开：填写。

## V2 接通之后才填写

NapCat账号与允许的测试对象：填写实际授权范围，不抄历史日志ID。
传输方式、服务运行位置：填写。
QQ适配器由谁开发、保存在何处：填写。
服务启动与聊天客户端是否同一生命周期：填写。
关闭本机聊天窗口后QQ是否继续：填写真实行为。
停用QQ而不破坏本机聊天的方法：填写。
本机与QQ私聊是否授权共享哪些记忆：填写；默认不能凭昵称共享。
当前不支持的消息类型和离线回补限制：填写。

## 本次实跑记录

日期、实际用户输入、角色实际回复、一个必要工具结果或错误：填写原文摘要并关联本机原记录。
还有什么不能用：填写。不要用 PASS 数量掩盖它。
