# M1 deterministic persistence slice

Status: PARTIALLY VERIFIED. The implementation is not yet all of V1.

Mongo-backed ingress, scoped context preparation, three separate character
phases, bounded protocol repair/recall, versioned state heads, CAS, publication
outbox, local idempotent receiver, hash-chain audit, static operator HTML and
state-only replay are implemented. The fake lane is only an engineering test
double; it is never evidence for real model quality.

Executable probe: `tools/probe_coordinator.py`, exit 0,
`reports/M1-probe-3825433e/result.json`. Real Mongo, three fake model calls,
100 duplicate deliveries, one receiver message, no public internal monologue.

Hardening: `tools/run_checks.py tests/test_engineering_m1.py -q`, exit 0,
`reports/check-20260918T140215Z-28ac10/`: 17 tests passed. Coverage includes
E01/02/03/04/05/07/09/10/14/15/17/19/21/23 subcontracts. E14 includes a real
child process exiting at receive-before-receipt and a fresh coordinator
recovering without a second message. This is only one of five required crash
locations. E23 uses actual Mongo collections and rejects modified event hashes.
Neither these tests nor a green count imply every clause of those IDs passed.

After the operator restarted the search server, the unchanged new vector index
became READY and the scope-prefiltered real query returned exactly the allowed
document: `M0-vector-reprobe-bc5e5bf6.json`. Earlier PENDING results remain.

Still not implemented/verified in this slice: native executor tools and task
feedback, DSH restart reconciliation, all five kill points, retrieval ranking,
full deletion propagation, reflection, native compaction, formal experiment
matrices, human review, and long-context performance. No formal PASS gate is
claimed. Full result mapping is deferred until these slices are exercised.
