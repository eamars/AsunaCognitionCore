# M7e — Browser evidence of partial effects and final rejection

The in-app Browser opened the saved real L09 audit trace. It displays the exact `DONE_WITHOUT_EVIDENCE` rejection, the final `FAILED_PROTOCOL` task document and the separately retained `simulated_copy_commit` receipt. Screenshots and DOM snapshots are hash-bound in `ui-qa-20260920-07/result.json`; all exported images have explicit visual review records.

This validates the operator display. It does not rerun the task, promote the failure to PASS, supply cognition votes or establish browser download completion. No application code changed, so no redundant regression rerun was required.
