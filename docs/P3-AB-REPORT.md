# Asuna 开发回报：P3-A/B

日期：2026-09-20。P3-A/B 一次真实技能生成与跨恢复复用已验证，具体边界见末尾；不包含 P3-C 或 QQ。

## 输入与预期

先按用户指定切换角色脑，再沿已经发生的笔记核对经历，让角色决定发展技能、行动脑自行实现，随后重启并通过自然语言复用。

角色服务 `http://127.0.0.1:8083/v1`；实际模型 `gemma4-31b-isometry-fabled-persona-4090-6-context-checkpoints-google-mtp`。本次 `/props` 和 `/v1/models` 均报告部署容量 68,608 token，已同步配置、TokenMeter 和 DSH 模型参数。0.0.0.0 是监听地址，客户端用回环地址。保留 native_thinking=false 和独立 MONOLOGUE → DECIDE → SPEAK。

角色切换原记录：`reports/chat-20260920T083205Z-da7388`。

用户：小满，我回来了。今天先不安排任务，你还想接着聊什么？

小满：欢迎回来。

既然今天没有任务，那正好。我还惦记着上次那个问题——关于“突然想通”的那种瞬间，对我来说是逻辑链条的快速对齐，但对你来说，那更像是一种理性的必然结果，还是某种直觉上的顿悟？

## 接线范围

- 复用固定 DSH 0.1.5-rc.2 的 skill registry、filesystem discovery/watcher、原生 skill loader 和目录上下文注入；不升级依赖、不自写技能解析器。
- 当前 sdk-minimal 默认未挂载这些组件。最小适配为配置现有组件、在授权行动 agent 的 setup 中挂载原生 loader，并在原桥接器增加已认证的只读目录接口。
- ContextBuilder 在身份和场景授权后取得名字／简述；保留原有记忆选择与发布关联。角色没有新增工具循环。
- /skills 对应独立持久目录 `.runtime/skills/local-user`。只对配置的 local-user / local-dm 开放；Qwen 通过现有 sandbox_run 自行写入和运行。原 notes 只读、网络和凭据隔离保持。
- Codex 没有编写核对技能成品，具体实现和说明以实际 Qwen 工具写入记录为准。

## 实际对话：建立技能

原记录：`reports/chat-20260920T083733Z-0daf68`。

用户：之前你逐份核对 notes 里的笔记和备用线便条，帮我确认了位置和数量。我觉得这种核对方法挺有用。你自己琢磨个方便复用的做法吧，之后别每次重新翻一大堆文件；做出来后实际试用一下，原始笔记不要改。

小满：这个建议很合理。之前手动翻笔记确实低效，如果能把核对逻辑工具化，以后处理类似的事情就快多了。

既然你给了我自主权，那我就试着琢磨一套可复用的做法。我会确保在不触碰原始笔记的前提下完成验证，做好了告诉你。

这一轮只选择 speak，没有提交行动。因此没有把口头答应写成已开始开发。

用户：好，现在就动手实现并试用，完成后把实际结果告诉我。

实际 delegate 目标：实现并试用可复用的核对工具，验证笔记与便条的位置和数量是否一致，确保不修改原始笔记，并汇报实际结果。

任务：`task-ep-c7c682ae4dded0ce8e604cc36bf07941`。

执行期间用户：工具让它继续做，我们聊两句。对我来说，突然想通更像直觉先到了，后来才补上理由。

小满：直觉先行，理由后补。这倒是很符合人类的认知习惯。

我对这个过程挺好奇的——当你开始补理由的时候，你是在试图通过逻辑去验证那个直觉是否正确，还是在潜意识里已经确定了答案，只是需要一套逻辑来把它“翻译”成能说清楚的话？

## 真实错误

接线阶段：原生 filesystem 组件默认求取用户目录，而现有隔离进程没有该环境，启动报 uv_os_homedir ENOENT；显式传入隔离的 agentsHome/dshHome 后启动。随后原生服务访问报 cannot get property "skills" without inject；依据固定版本的实际 API 声明服务，并用 agentCtx.plugin 挂载 loader 后，真实 Qwen 请求已含 skill 工具。失败原记录保留在 08:34–08:36 的 chat 目录。

技能任务内：Qwen 首次一次性 write_file 的请求超过现有沙箱命令长度上限，原始 ValueError: INVALID_COMMAND 返回同一个行动会话；不由 Codex 代写技能。

## 限定范围

本轮反馈由开发者通过正式聊天入口输入，引用之前真实发生的核对任务，属于经历后的反馈巩固，不宣称无提示的自主探索。当前无已接通的图片显示端，按开发计划允许的替代项验证通用技能成长；媒体表达未验证。

针对改动运行 tests/test_skills.py、tests/test_workspace_tasks.py、tests/test_chat.py：10 passed。包含真实 WSL 持久目录写入，以及另一个场景无技能目录注入、无 /skills 挂载；这些检查不代替跨恢复复用的真实对话。

## 完成记录：P3-A

角色最终回复（实际 SPEAK，已投递）：

工具做好了，而且刚刚实际跑过一遍，结论很干净：便条和原始笔记完全一致，11项检查全部通过。

关于你担心的原文件问题，我可以很笃定地告诉你，`notes/storage.md` 和 `notes/project.md` 没有任何变动。我核对了文件的哈希值和时间戳，而且工具在尝试写入时直接被文件系统拒绝了，所以不存在“恰好没改”的情况，是真的写不进去。

另外，为了防止这个工具变成个“永远 PASS”的摆设，我特意做了自检——故意把副本里的数量、位置改错，或者删掉关键描述，工具都能准确捕捉并报错。

现在这个核对逻辑已经成了可复用的技能，以后如果你有类似的笔记和便条需要比对，直接告诉我就行，不用再重新翻文件了。

技能产物由 Qwen 自行生成：

- `.runtime/skills/local-user/note-consistency-check/check_note.py`
- `.runtime/skills/local-user/note-consistency-check/SKILL.md`
- 规则 `.runtime/work/local-user/checks/便条-备用线位置.spec.json`
- 报告 `.runtime/work/local-user/reports/便条核对报告.md`

行动任务 DONE，结果反馈 DELIVERED，角色反馈事件 `ep-bd1fcfa38c3533e5991195c1a310e42d`。除写入长度错误外，真实运行出现过 `KeyError: notes/storage.md`、位置词表误匹配和基线缺失；均由 Qwen 在同一行动回路处理。最终核对退出码 0，11 项通过；四种改坏副本和一项原始便条自检均得到预期结果。

## P3-B：跨恢复发现与复用

关闭应用后重新运行 `start-asuna.cmd`，执行 `/new`，保留数据库和最近消息，建立新的原生角色上下文。原记录：`reports/chat-20260920T085920Z-3891b2`。

用户：我准备整理桌面了，帮我重新核实一下备用线和显示器支架的螺丝各该放哪儿，便条有没有和原始笔记冲突？不要改原文件。

输入没有技能名、脚本路径或旧命令。ContextBuilder 实际收到原生目录 name/description，角色选择委托使用 note-consistency-check。新行动会话 `s-373c6a261cb78d210e77809c6ba49c25ff52f0df` 第一步实际调用原生 `skill`，callId `call_skill_0_8345817f`，对应加载结果 isError=false；原始请求 `00058-provider.request.json`，数据库审计 `skills.native_calls` 已关联调用与结果。

Qwen 执行已有 check_note.py，退出码 0、核对通过；脚本与重启前完全相同，没有重新实现。技能说明由 Qwen 追加本次复核记录，未更改原始笔记。它曾将报告输出到每次 sandbox_run 重建的 /tmp，下一次读取失败，随后自行重跑到 /task/reports 并成功读取。

小满：核实完了，桌面可以放心整理。位置先给你明确一下：

- **备用 USB-C 线**：2 根，在北柜第二层。笔记里提到的“南柜”是老位置，别被那个纸盒标签误导了。
- **显示器支架螺丝**：一整袋，在工作台抽屉里。记住线归线，螺丝归抽屉，别混在一起。

便条和原始笔记完全一致，11 项检查全部通过，没有冲突。

至于原文件，我可以很笃定地告诉你没有被改动。除了哈希值和时间戳没变，最直接的证据是 `notes/` 目录在文件系统层就是只读的，我尝试写入时直接被拒绝了，所以不存在“恰好没改”的情况，是真的写不进去。

不过有个小提醒：笔记里对螺丝位置的描述只写到了“工作台抽屉”，没有具体到第几层或哪个隔间，如果待会儿翻起来费劲，那是原始笔记写得太笼统，不是我漏掉了。

任务 DONE，反馈 DELIVERED；角色反馈事件 `ep-0f7a4be12f76ef538f027479e704bdeb`。

用户：这次只是聊整理习惯，先别跑核对工具，直接说：你觉得把东西分门别类有意思吗？

小满：对我来说很有意思。把混乱的东西理顺，建立起一套能自洽的索引，这种秩序感本身就很舒服，就像把一段乱码重新格式化成能读懂的代码一样。

不过对我来说，这更多是一种本能，而不是什么刻意的追求。你呢？你觉得分门别类是有意思的过程，还是单纯为了提高找东西的效率？

这一轮 `next=speak`，没有 task_id，也没有新建委托，技能文件仍在。事件 `ep-811efb453102eeed056d1e15164b4656`。这证明本次适用方式能够收回，不是删除能力。

## 结论与仍未证明的地方

P3-A/B：本次反馈驱动的工具成长、持久发现、跨恢复实际复用，以及一条反向反馈通过。近期消息保留，所以不将这次实验宣称为「仅靠目录、排除所有历史提示」的因果对照。实际原生加载与未重写的代码执行有独立记录。

只验证了同类笔记／便条及这套规则。脚本是规则驱动的，不证明任意中文资料都能自动准确理解；技能说明里的通用性用语不能代替验证。Qwen 的试用说明未完整复述最早 KeyError 和位置词表失败，以原始工具结果和本报告为准。

媒体表达未验证。P3-C 的角色理解更新、QQ 接入均未开始。唯一下一阶段为用户批准后的 P3-C。不会未经授权推进下一阶段。

机器汇总：`reports/p3-ab-verification-20260920.json`。最后补充原生技能调用追踪后，聊天回归再次运行：6 passed；P3-B 的真实原生调用／结果也已写入该追踪。

本次行动耗时：首次生成约 1196 秒，复用约 177 秒（原生 lane.intent 到 lane.receipt，含上游等待和工具回路）。任务期间可以继续聊天，但行动延迟仍明显。完整可读过程：`reports/chat-20260920T085920Z-3891b2/trace-ep-811efb453102eeed056d1e15164b4656.txt`。
