# M3 controlled executor and effect boundary

Real two-model probe `M3-20260918T142244Z-b26d8a` passed: Gemma delegated a copy,
Qwen selected actual native DSH tools to inspect/copy/hash/commit it, the original
remained unchanged, and a new Gemma feedback episode published via PublishService.
Qwen cannot call publication or receive Mongo credentials. The broker binds the
DSH session to a trusted task lease; tool arguments cannot choose another task.
Each tool call has intent, receipt and request content hash. Missing receipts,
stale fences, cancellation, unknown delivery and the 64-tool budget fail closed.

All earlier attempts remain: migration was interrupted by a Mongo restart;
another model result was rejected for using a path as an artifact reference.
The final schema repair is at most one same-Qwen call, with every rejected output
retained. It never changes character monologue or public text.

`check-20260918T142555Z-aec8e2` had two failures. One was the Mongo server crash;
the other exposed WSL shell reconstruction of punctuation-bearing argv before
bubblewrap. `Sandbox` now uses `wsl --exec`; a direct file-write probe and the
full `check-20260918T142904Z-5214b8` then passed (23 tests, real Mongo, six real
sandbox/broker checks). Earlier sandbox evidence was narrower and did not prove
this argv boundary. No shell wrapper remains between untrusted argv and bwrap.

Mongo's 1024 soft file limit caused WiredTiger `Too many open files` and panic.
Under the user's repair authorization the live mongod/entrypoint limits were
raised to 64000 (hard limit unchanged), and Compose gained 64000 limits for its
next recreation. The original Compose was backed up with mode 0600. No container
was recreated and no database was deleted or repaired. See search-diagnostics
`20260918T142903-mongo-nofile-repair.json` for exact before/after hashes and command
exit. A simple Docker restart retains the existing container configuration;
Compose recreation uses the persisted setting.

This stage is not the ten-run L02, five-run L03 or reconciliation L12 acceptance.
Process failure coverage remains partial; in particular uncertain general code
execution stops for reconciliation rather than promising exactly-once effects.
