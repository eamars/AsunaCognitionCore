# M4 scoped retrieval and bounded memory updates

`M4-20260918T142348Z-bceacf`: real local embeddings, READY Mongo vector index,
12 preregistered queries and 200 seeded distractors. Recall@6 = 1.0 and every
critical query hit its target. The first attempt `M4-20260918T142156Z-5e2722`
failed at 0.8333. Its traces exposed lexical boilerplate overwhelming vector rank
and a missing trusted display-name/entity lookup. The new run filters corpus-wide
lexical boilerplate and uses identity aliases, retaining the specified RRF formula
`sum(1/(60+rank))`. No gold data enters retrieval or model requests. Fixtures and
thresholds are unchanged; code and experiment fingerprints differ.

Candidates are prefiltered server-side by scope, character, epoch, status and
embedding routing revision, then authoritatively reread. Corrections retain their
marked historical source. Pending records use separately labelled exact backread;
that path is not counted as vector recall. Recent chat source IDs are deduplicated.
Persona and relationship baselines bypass top-k. There is no query-result cache.

`M4-reflect-20260918T143638Z-632822` passed one real bounded Gemma reflection;
no_change is allowed. State proposals validate old/new values, CAS, scope and
allowed content fields. Processed source-event IDs prevent repeated retrieval or
retry being counted as new social evidence. Chat chunks are scoped, 2–4 messages,
zero overlap, with a conservative UTF-8-byte token upper bound.

`check-20260918T143537Z-58efad`: 26 tests passed using real Mongo. This does not
claim complete E20 deletion propagation or the full L04/L05/L10 suites. Embedding
weight revision is still unavailable; the recorded routing fingerprint is not a
weight hash. Recall success alone does not establish natural-language cognition.
