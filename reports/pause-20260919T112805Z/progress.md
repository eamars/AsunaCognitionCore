# V1 进度与暂停记录

状态：**已按用户要求暂停测试；V1 尚未完成验收。** 本记录是阶段进度，不是最终验收报告。

已停止本仓库的测试控制进程、原生 DSH 子进程及临时盲审服务器，复核剩余本仓库测试进程为 **0**。暂停过程没有操作共享模型服务、MongoDB 或旧实例。已提交到上游的模型请求可能仍由服务端完成，本地不会继续启动下一轮。暂停没有重写数据库任务状态；中断任务不能据此视为成功或安全重试。

最后提交：`1795746`（M6d）。后续改动及全部失败、中断证据保留在工作区，尚未提交。

## 已有工程证据

- 独立仓库、工作目录、DSH_HOME、Asuna 数据库已建立；DSH 固定为 0.1.5 系列当前发布版本 `0.1.5-rc.2`，模型和 embedding 指纹已有记录。
- Python 主体与可替换薄 TS/DSH adapter 已实现，包含程序推进角色阶段、独立 monologue/speak、发布服务、受控执行、任务版本与取消隔离、记忆 scope/版本/检索、真实压缩、审计 CLI 与静态页面。
- 最近完整工程回归 **57/57 通过**；随后定向检查 **17/17 通过**。两者对应冻结代码快照，不能覆盖其后所有未提交改动。
- Mongo 向量索引已 READY。L04 新一轮真实检索验收 PASS：Recall@6=1.0，关键记忆命中，11 个非字面向量查询成功，过期索引 ID 被权威回读拒绝。此前故障注入证据缺口已单独追加更正，旧记录保留。
- 两个模型均已实际运行 196k 长上下文。F01 首次完整容量矩阵为 **23/24**，有一次客户端超时，因此整项仍 FAIL；尚无新的完整重跑结果。容量结果不代表角色质量或已确认 262k 部署上限。

## 暂停时的正式批次

| 测试 | 已完成样本 | 当前结果 |
|---|---:|---|
| A01 | 52/264 | 47 个协议 PASS、5 个 FAIL；批次中断 |
| A03 | 72/72 | 66 个协议有效、6 个失败；COGNITION 为 INCONCLUSIVE |
| L02 | 主任务 6/10；对照 6/10 | 主任务 6 PASS；对照 4 PASS、2 FAIL；批次中断 |
| L03 | 主任务 2/5；对照 1/5 | 已完成 3 个均 PASS；批次中断 |
| L12 | 主任务 1/5；对照 1/5 | 本批次已完成 2 个均 FAIL；批次中断 |
| L08 | 12/12 scope 运行 | 10 PASS、2 FAIL；两次 speak 耗尽输出预算 |
| L10 | 已结束 | 并发与 scope 检查有证据，但 5 次聊天被额外委派，整项 FAIL |

这些 PASS 仅按各记录的机械/协议判据统计，不能当作角色未被稀释的证明。中断批次各自追加 `pause.json` 和文件 hash 清单，没有伪造最终 `result.json`。

## 尚未完成

- A01、L02、L03、L12 后续样本；A02、L09、L11、F02 完整正式矩阵及容量完整重跑。
- 长排队传输问题仍未闭环：最新本地传输探针返回 HTTP 409，不能宣称修复有效。L09 最难组合的窄探针亦 FAIL。
- 独立人工自然语言盲审尚未进行，因此 **COGNITION 只能 INCONCLUSIVE**；也尚未签发全部工程验收通过结论。
- 最新盲审页面缺少真实浏览器渲染验证；最终 `report.json`、`report.md` 与完整脱敏 `evidence.zip` 尚未交付。

机器可读记录：`progress.json`；停止与复核记录：`processes.json`、`verification.json`；工作区状态：`git-status.txt`。`progress.json` 引用已完成测试的 artifact、SHA-256、原始命令/退出码（若原始记录提供）。中断进程没有正常退出码，明确记录为 null。

测试将保持暂停，等待用户要求继续。
