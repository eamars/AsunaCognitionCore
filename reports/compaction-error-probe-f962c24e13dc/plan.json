{
  "boundary": "installed DSH engine, production bridge, real Mongo persistence",
  "substitution": "deterministic loopback model output; no real-model quality claim",
  "required": [
    "native rejects summary larger than history",
    "exact accepted message yields error with empty output and native cause",
    "failed summary does not become successful compaction",
    "replay returns same error without another provider call"
  ]
}