# M7c — L09 hardest-condition failure

The real probe `matrix-probe-0b783bca1b` completed three character and five executor native summaries. Its protected source files match the frozen fixtures. The produced report has 668 unique posted records and 935122 cents, and two actual simulated-copy commit receipts exist.

The first executor output used a fenced, incompatible result schema. Its one allowed same-model repair returned valid JSON declaring `done` with a nonempty `unmet_items` array. The existing TaskService correctly rejected it with `DONE_WITHOUT_EVIDENCE`, leaving `FAILED_PROTOCOL`. No successful final task feedback or later continuity answer exists. Files and summary counts cannot promote this failed chain to PASS.

`tools/diagnose_matrix.py` independently preserves the actual output, terminal state, exact summary joins and generated files. The diagnostic passed with exit 0 and zero model calls/database writes; the source probe exited 1 and remains FAIL.

The earlier execution plan gated the full L09 matrix on a successful task/oracle probe. We revise that work-order gate: the real transport and exact native counts are now demonstrated, while this observed model-result failure must remain part of the assessment. The full matrix may proceed without first selecting a passing probe. No source fixture, persona, sampling setting or acceptance threshold changes.

At this checkpoint A01 and L02 are running in fresh experiments, with at most two own workflows. Full L09 and the other remaining matrices are still pending. No independent human cognition votes have been supplied.
