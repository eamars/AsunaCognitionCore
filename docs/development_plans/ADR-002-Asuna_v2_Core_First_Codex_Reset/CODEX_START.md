# Codex：本次从这里执行

在现有 `asuna_cognition_core_v2` 上继续。用户尚未开始执行上一轮纠偏，本包是合并后的唯一当前执行入口。不是新项目初始化，也不要求先做旧文档的全部步骤。

## 最新阶段状态（2026-09-20）

P1-A/B/C、P1-D/P2 的限定验证已完成；P3-A/B 本次已通过真实技能生成、重启后的原生发现与复用及反向反馈，详见 `docs/P3-AB-REPORT.md`。P3-C 已按用户授权完成一次真实关系理解修订、恢复注入和另一人物隔离，见 `docs/P3-C-REPORT.md`。最终交付见 `docs/V1-FINAL-DELIVERY.md`。当前停止在 V1 交付点，V2 等用户主动启动。模型上下文最后实查为 68,608 token。下文保留原主线目标和规则，不重复启动已完成阶段。

## 当前第一项任务

交付收尾按用户最后指示执行：不脱敏，完整本地工作区（不含 `.git`）原样归档到 `C:\workspace\asuna-v1-core-raw-20260920.zip`，验证后工作区只保留可提交内容。配置、依赖、运行目录及历史记录在包内；再次运行前按 `RUN_ASUNA.md` 恢复。外部 Mongo 不清理。不要把清理后的依赖缺失误判为阶段功能回退。

**接通一个用户本人能持续使用的本机自然语言入口。**

默认命令为待实现的 `asuna chat`。一次启动保持 Application、角色场景与所需 DSH 会话；用户输入中文，只看到角色真实公开发言。`/trace` 展开真实记忆、独白、委托、工具过程和原始错误。不要先做新 Web UI，也不要把 `run --text ...` 的 JSON 输出包装成“聊天已完成”。

先按需要读 `cli.py`、`application.py`、`coordinator.py`、`router.py`、`tasks.py`、`memory.py`、`retrieval.py`、`dsh_lane.py`。已有代码若已修好，不回退。完成 P1-A 后立即交回实际启动办法与一段原文，用户可先试聊；这不等于 V1 全部完成。

## 不允许改变的架构

- **Gemma 31B** 是当前角色主体（用户于 2026-09-20 指定替换原 Gemma 4 26B；本机 `http://127.0.0.1:8083/v1`，模型 `gemma4-31b-isometry-fabled-persona-4090-6-context-checkpoints-google-mtp`，当前部署上下文 68,608 token）。程序推进有界的 monologue／意图／发言阶段；她决定态度、价值取舍与表达，不负责自主维持工具循环。
- **Qwen3.8 Flash** 是更自主的行动脑。自行获取数据、选择工具、编码、修任务错误和执行长流程；不代写角色台词，不代替她决定情绪。
- 复用现有两条模型路由；角色侧以用户指定的新本机服务及其实际容量为准，行动侧配置保留。没有用户指令或实际阻塞，不升级 DSH、换模型、重建数据库或切换技术栈。沿用 Python 业务层＋已有薄 TS 扩展。
- 人格、关系与相关记忆由程序可靠送达；新聊天、独白、实际行动进入长期存储。上下文、长期记忆和缓存不是同一种东西。
- 人格、关系理解、记忆和技能允许随经历成长。固定的是权限及程序推进规则，不是所有可能的行为。

## 开发顺序（不准自行交换）

1. **P1-A 本机持续角色聊天。**
2. **P1-B 聊天→自主行动→结果→继续聊天。** 默认授权工作区由配置提供，不让用户手工运行 executor。
3. **P1-C 新聊天自动入库、索引、真实召回。**
4. **P1-D 一次压缩／恢复与基本场景连续性。**
5. **P2 最小必要异常恢复。** 沿 DSH／Qwen 原回路处理实际任务错误。
6. **P3 基础成长。** 一次实际技能成长及一次由角色形成的理解更新，后续可复用。
7. **交付 V1；V2 待用户启动。** V2 从本机聊天让 Asuna 自己开发 NapCat 适配器；先读 `V2_NAPCAT_TASK.md`，不要自行写 QQ 成品。

每个阶段只读相关章节和对应对话。P1-A 不需要先阅读、实现整个 V2 协议。P1 失败时不得转做 UI、美化、哈希清单、性能矩阵或更多边界测试。

## 防止再次偏移的工作规则

每次修改先用三句话说明：**哪个真实交互不成立；准备修哪里；用哪句自然语言复查。** 说不出它如何推进当前阶段，暂不做。

每阶段最多一个当前阻塞、一个紧邻验证。只重跑改动影响的对话，不不断换 seed 追求漂亮样本，不为了测试句子反复叠提示词。先检查真实注入、路由、工具接线和发布；有具体语义误解才做最小 prompt 修改。

monologue、公开回复、Qwen 结果均为自然语言；只保留程序真正需要的极小内部决策。标识、版本和回执由代码附加。既有内部哈希可以不动，但不再把它们当成果或让模型逐项抄写。

错误不等于整轮失败。任务内 traceback 交回原行动会话，Qwen 自己修。Codex 修运行时接线和基础设施缺口，不手工替 Asuna 完成任务，也不把所有错误替换为泛化诊断。

保留身份／场景、授权、凭据隔离和不虚报完成的底线。不能从独白里的情绪化句子猜出破坏性执行命令，也不能将“准备说”提前记为“已经说”。

## 交回给用户的东西

用 `REPORT_TEMPLATE.md` 回报真实输入／输出、必要 traceback、这次记住或改变什么、仍缺什么、唯一下一步。不要用百分比或测试数代替产品状态。

按 `LOCAL_ENTRY.md` 在本机生成一份已填写的 `RUN_ASUNA.md` 和一个实际可运行的启动脚本。必须在用户能访问的终端／宿主机验证，不只在测试进程内验证。最终给出的命令不得还有 `<容器名>`、`<项目目录>` 等占位符。

**本次第一份有价值的交付是一个能聊的入口，不是一份新的架构报告。**
