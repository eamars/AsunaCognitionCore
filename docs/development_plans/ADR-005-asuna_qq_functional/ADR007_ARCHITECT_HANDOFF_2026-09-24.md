# 给 ADR-007 架构师的衔接事实

> 2026-09-25 后续待裁定问题：用户要求本地聊天的开发与集成能力像 DSH 基线一样默认可用，同时保持 QQ 授权隔离不变。完整问题、当前机制、Codex 已撤回草稿及请架构师裁定的事项见 [ADR-007 本地能力交接](../ADR-007-asuna_autonomous_self_development/ARCHITECT_HANDOFF_2026-09-25_LOCAL_CAPABILITIES.md)。

2026-09-24。ADR-007 的开发目标仍由架构师编写；本文只交代当前状态和用户新指示，不代写 ADR-007 方案。

- 当前 Asuna 沿用现有 Web、角色脑/行动脑、DSH 工具执行和反馈 loop。ADR-005 的 P1 已部署到现有宿主：QQ 对方资料、授权历史查询与来源回读、按需讨论整理。正式 Web 已跑通本机场景的角色委托、ToolBroker 查询和公开答复；真实 QQ 群使用不由这项本机证据代替。
- ADR-005 后续顺序由用户定为 **P2 → P5 → P3**。三片已由小满自测后依次部署到现有宿主；P3 于 2026-09-25 完成机械应用和维护重载，记录见 [ADR005_P3_DEPLOYMENT_2026-09-25.md](ADR005_P3_DEPLOYMENT_2026-09-25.md)。随后用户又指定群 `1124198125` 接入并启用 P5，部署事实见 [ADR005_P5B_DEPLOYMENT_2026-09-25.md](ADR005_P5B_DEPLOYMENT_2026-09-25.md)；当前主动模式在 `1002866238` 与 `1124198125` 两群开启。原 P4 读图/表情包已移出 ADR-005，不再作为这些片段的前置条件。
- 小满继续编写产品代码、宿主接线、必要检查和说明。小满交付完成的可应用改动后，Codex 按用户授权直接机械部署和必要维护重载，不另做独立验收验证，也不代写业务代码。
- 用户对 ADR-007 的要求是**大幅减少给小满的具体指示**。发布判断聚焦于她原有执行 loop 能否继续；普通功能细节和非阻断性问题不设成发布总闸。除这条原则外，ADR-007 的目标和架构选择待架构师确定。

## 用户对下一项任务的进一步授权

从 ADR-007 开始，用户要求向小满提供**未脱敏的数据和真实数据库**，不再把生产库与测试库分开；这项变更不追溯到已完成的 ADR-005 自测。届时按真实宿主交付访问所需信息，不在本衔接文件写入凭据或未脱敏内容。

用户同时明确：Codex **不再对小满开发的产品托底**。小满对自己的实现、测试与普通故障继续负责；Codex 不以代写、代测或补救实现代替她的执行过程。ADR-007 的具体开发目标仍由架构师编写。

### 当前宿主事实，供安排真实库接入

- 现有宿主的 Mongo 位于内网 `192.168.2.10:27027`，当前业务库名是 `asuna_cognition_core_v2`；现有 `mongo_uri` 没有用户名。实际完整配置保留在未入库的 `config/local.json`，不复制到本文。
- 小满现用的 `integration_dev` 是无网络开发目录；`integration_test` 只通配置过的 TCP 转发。目前 `config/integration.local.json` 仅有 `napcat` 与 `host` 两个端点，没有 Mongo。因此只给她一个 URI 字符串，仍不能在现有隔离进程连接真实库；当前隔离系统也没有 `pymongo`、`pytest`。ADR-007 开始时需要把实际可用的访问路径及依赖一并交付，仍走现有宿主/ToolBroker 授权，不新开公开数据库入口。
- 当前 P2 的真库测试夹具会在目标库前后清空本套件使用的固定集合，原说明要求显式覆盖为隔离库 `asuna_v2_test_p2_host_20260924`。ADR-007 不再区分生产/测试库后，不能直接把这类夹具照搬到真实业务库运行；这是现存脚本的具体副作用，不是 Codex 对 ADR-007 产品的验收门槛。由小满在新目标下决定自己的非破坏性验证方式。
